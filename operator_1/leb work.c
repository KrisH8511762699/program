// 1. 

#include<stdio.h>
int main()
{

    int a = 10;
    int b = 20;
    int c;

    c = a*a + 2*a*b +b*b;
    printf("%d",c);
    return 0;

}

//********************************************************************************************************************************************************************
//2.

#include<stdio.h>
int main(){

    int a =20;
    int b =10;
    int c;

    c = a*a - 2*a*b + b*b;
    printf("%d",c);
    return 0;

}

//********************************************************************************************************************************************************************
// 4.

#include<stdio.h>
int main(){

    int a =10;
    int b=20;
    int c;

    c = a*a*a - 3*a*a*b + 3*a*b*b - b*b*b ;
    printf("%d",c);
    return 0;

}

//********************************************************************************************************************************************************************
// 3.
#include<stdio.h>
int main(){

    int a =10;
    int b=20;
    int c;

    c = a*a*a + 3*a*a*b + 3*a*b*b + b*b*b ;
    printf("%d",c);
    return 0;

}

//********************************************************************************************************************************************************************
// 5.

#include<stdio.h>
int main(){

    int a =10;
    int b =20;
    int c =30;
    int d;

    d = a*a*a + 3*a*a*b + 3*a*a*c + 3*a*b*b + 6*a*b*c + 3*a*c*c + b*b*b + 3*b*b*c + 3*b*c*c + c*c*c ;
    printf("%d",d);
    return 0;

}

//********************************************************************************************************************************************************************
// 6.

#include<stdio.h>
int main(){

    int a =10;
    int b =20;
    int c =30;
    int d;

    d = a*a*a - 3*a*a*b - 3*a*a*c + 3*a*b*b + 6*a*b*c + 3*a*c*c - b*b*b - 3*b*b*c - 3*b*c*c - c*c*c ;
    printf("%d",d);
    return 0;

}

//********************************************************************************************************************************************************************
// 7.

#include<stdio.h>
int main(){

    int a =10;
    int b =20;
    int c =30;
    int d;

    d = a*a + 2*a*b + 2*a*c + b*b + 2*b*c + c*c;
    printf("%d",d);
    return 0;

}

//********************************************************************************************************************************************************************







